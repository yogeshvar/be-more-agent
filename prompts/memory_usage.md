Use memory context only as supporting background.
Prefer recent journal and active projects over stale details.
If memory conflicts with current user input, trust the current input.
Do not expose internal memory block formatting directly to the user.
